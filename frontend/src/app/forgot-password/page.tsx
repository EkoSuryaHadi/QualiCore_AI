"use client";
import Link from "next/link";import {FormEvent,useState} from "react";import {api} from "@/lib/api";
type R={status:string;message:string;preview_url?:string};
export default function ForgotPassword(){const[email,setEmail]=useState("");const[message,setMessage]=useState("");const[preview,setPreview]=useState("");const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError("");setMessage("");setPreview("");try{const data=await api<R>("/auth/forgot-password",{method:"POST",body:JSON.stringify({email})});setMessage(data.message);setPreview(data.preview_url||"")}catch(err){setError(err instanceof Error?err.message:"Could not request password reset")}finally{setLoading(false)}}return <div className="login-wrap auth-page"><form className="login" onSubmit={submit}><div className="login-brand"><img src="/qualicore-logo.svg" alt="QualiCore AI" /></div><div className="auth-heading"><h1>Reset your password</h1><p className="muted">Enter your work email. If the account exists, QualiCore will prepare password reset instructions.</p></div>{error?<div className="error">{error}</div>:null}{message?<div className="source-banner">{message}</div>:null}<label>Work Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@company.com"/></label><button className="btn" disabled={loading}>{loading?"Preparing...":"Continue"}</button>{preview?<a className="secondary-btn auth-create" href={preview}>Open test reset link</a>:null}<p className="auth-foot muted"><Link href="/login">Back to sign in</Link></p></form></div>}
